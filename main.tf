module "vpc" {
  source                        = "git::https://github.com/Raphaelfcs/vpcaws.git"
  ENV                           = "${var.ENV}"
  AWS_REGION                    = "${var.AWS_REGION}"
  CIDRVPC                       = "${var.CIDRVPC}"
  requerente                    = "${var.requerente}"
  map_public_true               = "${var.map_public_true}"
  network_DMZ_A                    = "${var.network_DMZ_A}"
  CIDR_network_DMZ_A               = "${var.CIDR_network_DMZ_A}"
  network_DMZ_B                    = "${var.network_DMZ_B}"
  CIDR_network_DMZ_B               = "${var.CIDR_network_DMZ_B}"
  network_DMZ_C                    = "${var.network_DMZ_C}"
  CIDR_network_DMZ_C               = "${var.CIDR_network_DMZ_C}"
  CIDR_network_DMZ_D               = "${var.CIDR_network_DMZ_D}"
  network_DMZ_D                    = "${var.network_DMZ_D}"
  network_APP_A                    = "${var.network_APP_A}"
  CIDR_network_APP_A               = "${var.CIDR_network_APP_A}"
  network_APP_B                    = "${var.network_APP_B}"
  CIDR_network_APP_B               = "${var.CIDR_network_APP_B}"
  network_APP_C                    = "${var.network_APP_C}"
  CIDR_network_APP_C               = "${var.CIDR_network_APP_C}"
  network_APP_D                    = "${var.network_APP_D}"
  CIDR_network_APP_D               = "${var.CIDR_network_APP_D}"
  instance_tenancy              = "${var.instance_tenancy}"
  enable_dns_support            = "${var.enable_dns_support}"
  enable_dns_hostnames          = "${var.enable_dns_hostnames}"
  enable_classiclink            = "${var.enable_classiclink}"
  name_vpc                      = "${var.name_vpc}"
  cluster-name                  = "${var.cluster-name}"
}